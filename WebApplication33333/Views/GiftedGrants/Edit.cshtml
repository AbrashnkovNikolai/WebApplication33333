@model WebApplication33333.GiftedGrant

@{
    ViewData["Title"] = "Edit";
}

<h1>Редактирование</h1>

<h4>GiftedGrant</h4>
<hr /> 



<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger">Idстипендии</div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Id" class="control-label">Idстипендии</label>
                <input asp-for="Id" class="form-control" />
                <span asp-validation-for="Id" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="GrantName" class="control-label"> Название стипендии</label>
                <select asp-for="GrantName" class="form-control" asp-items="ViewBag.GrantName"></select>
                <span asp-validation-for="GrantName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="GrantValue" class="control-label"></label>
                <input asp-for="GrantValue" class="form-control" />
                <span asp-validation-for="GrantValue" class="text-danger">размер стипендии</span>
            </div>
            <div class="form-group">
                <label asp-for="StudentId" class="control-label"></label>
                <select asp-for="StudentId" class="form-control" asp-items="ViewBag.StudentId">
                    Номер студенческого билета
                </select>
                <span asp-validation-for="StudentId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
